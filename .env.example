# Supabase Config
# Get these from your Supabase Dashboard: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"

# Service Role Key (Required for server-side atomic operations bypassing RLS)
# WARNING: Keep this secret! Do not expose to the client.
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Optional: Set to 1 to enable deterministic time mocking via x-test-now-ms header
# TEST_MODE=1
